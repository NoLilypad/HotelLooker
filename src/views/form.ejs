<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Choix de la semaine et du nombre d'adultes</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="form-box">
        <h1>Recherche de prix Booking.com</h1>
        <form method="POST" action="/prices">
            <label for="start_date">Semaine</label>
            <input type="date" id="start_date" name="start_date" value="<%= days[0] %>" required>
            <label for="adults">Nombre d'adultes</label>
            <select id="adults" name="adults">
                <% for(let i=1; i<=3; i++) { %>
                    <option value="<%= i %>" <%= i === defaultAdults ? 'selected' : '' %>><%= i %></option>
                <% } %>
            </select>
            <button type="submit">Voir les prix</button>
        </form>
    </div>
</body>
</html>
