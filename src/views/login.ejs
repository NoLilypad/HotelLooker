<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Connexion</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 2em; }
        .login-box { max-width: 350px; margin: 3em auto; border: 1px solid #ccc; padding: 2em; border-radius: 8px; }
        h1 { text-align: center; color: #2a4d8f; }
        .error { color: #b00; margin-bottom: 1em; text-align: center; }
        label { display: block; margin-top: 1em; }
        input[type="text"], input[type="password"] { width: 100%; padding: 0.5em; margin-top: 0.2em; }
        button { margin-top: 1.5em; width: 100%; padding: 0.7em; background: #2a4d8f; color: #fff; border: none; border-radius: 4px; font-size: 1em; }
    </style>
</head>
<body>
    <div class="login-box">
        <h1>Connexion</h1>
        <% if (error) { %>
            <div class="error"><%= error %></div>
        <% } %>
        <form method="POST" action="/login">
            <label for="username">Nom d'utilisateur</label>
            <input type="text" id="username" name="username" required autofocus>
            <label for="password">Mot de passe</label>
            <input type="password" id="password" name="password" required>
            <button type="submit">Se connecter</button>
        </form>
    </div>
</body>
</html>
